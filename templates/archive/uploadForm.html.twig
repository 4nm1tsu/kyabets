{% extends 'base.html.twig' %}

{% block title %}Kyabets-Archive{% endblock %}

{% block body %}
<h3 class="text-center text-info">Upload form</h3>
    <div class="container">
        <div class="row justify-content-center align-items-center">
            <div class="col-md-6">
                <div class="col-md-12">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
