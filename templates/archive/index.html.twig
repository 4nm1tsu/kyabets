{% extends 'base.html.twig' %}

{% block title %}Kyabets-Archive{% endblock %}

{% block body %}
<div class="container">
    <div class="card text-white bg-secondary mb-3 mx-auto " style="max-width: 40rem;">
        <div class="card-header">This page is not available</div>
        <div class="card-body">
            <h4 class="card-title">Sorry...</h4>
            <p class="card-text">I'll do my best to deal with that as soon as possible.</p>
        </div>
    </div>
</div>

<h3 class="text-center text-info">Upload form</h3>
    <div class="container">
        <div class="row justify-content-center align-items-center">
            <div class="col-md-6">
                <div class="col-md-12">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
